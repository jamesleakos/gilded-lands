import styled from 'styled-components';

export const RealmMapStyled = styled.div`
  position: relative;

  // placeholder to get the map to show up. Should be overwritten BY CSS in parent component
  min-height: 100px;
  min-width: 100px;
`;
